from django.conf.urls import url

import Analytics
from Analytics.views import get_bus_arrival,index, BusArrivalV2
from Analytics import views
# from django.urls import path
urlpatterns = [
    # url('',index),
    # url(r'^analytics_bus_arrival/', views.BusArrivalV2, name='bus/'),
    # url('/get_bus_arrival',views.get_bus_arrival,name='get_bus_arrival/'),
    # url(r'^test/$',test)

]
